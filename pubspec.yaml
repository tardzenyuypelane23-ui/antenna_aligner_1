name: antenna_aligner
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.5

dependencies:
  ar_flutter_plugin_2: ^0.0.3
  flutter:
    sdk: flutter
  vector_math: ^2.1.4
  geolocator: ^12.0.0
  flutter_compass: ^0.8.1
  flutter_blue_plus: ^1.31.0
  permission_handler: ^11.3.1
  sqflite: ^2.3.0
  path: ^1.9.0
  wakelock_plus: ^1.6.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
